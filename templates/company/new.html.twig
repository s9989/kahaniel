{% extends 'base.html.twig' %}

{% block title %}New company{% endblock %}

{% block body %}
    <section>
        {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </section>
{% endblock %}
